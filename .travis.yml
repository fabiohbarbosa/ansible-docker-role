sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t ansible-docker-role tests/

script:
  - sh test.sh

notifications:
  email: false
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
